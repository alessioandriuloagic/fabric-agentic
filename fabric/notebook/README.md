# Notebook Artifacts

S1-00 will add a versioned CRM extraction notebook that stages `crm_demo/accounts` data under
`Files/raw/` and returns structured extraction counts. It must not write Bronze directly.
